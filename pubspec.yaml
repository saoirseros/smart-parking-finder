name: smart_parking_finder
description: A Smart Parking Finder App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.5.0
  firebase_core: ^2.30.0
  firebase_database: ^10.5.0
  qr_flutter: ^4.1.0
  uuid: ^4.3.3
  flutter_dotenv: ^5.1.0

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env